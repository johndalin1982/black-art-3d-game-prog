# StarBlazer (32-bit DOS/4GW, IPX) - JOIN. Run from repo root; then MAKE CONNECTION (Enter at prompt).
# Audio off here on purpose - the HOST instance plays sound/music.
[ipx]
ipx=true
[autoexec]
@echo off
mount c blzrx32
c:
ipxnet connect 127.0.0.1
blzrx32
