# StarBlazer (16-bit, IPX) - JOIN. Run from repo root; then MAKE CONNECTION (Enter at prompt).
# Audio off here on purpose - the HOST instance plays sound/music (avoids double audio).
[ipx]
ipx=true
[autoexec]
@echo off
mount c blzrx
c:
ipxnet connect 127.0.0.1
blzrx
