# StarBlazer (32-bit DOS/4GW, VESA 640x480, IPX) - JOIN. Run from repo root; then MAKE CONNECTION (Enter at prompt).
# Audio off here on purpose - the HOST instance plays sound/music.
[dosbox]
machine=svga_s3
[ipx]
ipx=true
[autoexec]
@echo off
mount c blazerx
c:
ipxnet connect 127.0.0.1
blazerx
